# =============================================================
# OncoFix Frontend Configuration
# These are baked into the build. After changes, rebuild the frontend:
#   cd /opt/oncofix/health-intake-form && npm run build
#   sudo systemctl reload nginx
# =============================================================

VITE_API_BASE_URL=http://localhost:443/api/v1
VITE_PUBLIC_ACCESS_TOKEN=oncofix-public-token-2024-demo
